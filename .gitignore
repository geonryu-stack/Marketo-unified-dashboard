# PHP 설정 (민감 정보)
config/config.php
config/marketo_token.cache

# Next.js 환경 변수 (민감 정보, 레거시)
.env.local
.env*.local

# 런타임 데이터
data/

# macOS
.DS_Store

# local Claude Code settings (personal, not project)
.claude/settings.local.json
